# Mix engine — ≥10 channels

## Requirements
- Minimum **10** realtime channels
- Simultaneous playback on **OS (PC)** first-class
- Per-channel mute/solo/gain; master bus
- Works with sync-kernel (±0.2 budget)

## Web
Meters + transport UI; audio work where possible via AudioWorklet; heavy mix prefers OS host.
