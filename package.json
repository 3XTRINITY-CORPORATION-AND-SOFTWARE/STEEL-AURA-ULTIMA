{
  "name": "steel-aura-ultima",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test:sync": "tsx --test packages/sync-kernel/src/smoke.test.ts",
    "test:mix": "tsx --test packages/steel-core/src/mix-engine.test.ts",
    "test": "npm run test:sync && npm run test:mix"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^20.0.0"
  }
}
